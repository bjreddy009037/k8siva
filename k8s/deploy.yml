apiVersion: apps/v1
kind: Deployment
metadata:
  name: first-deploy
spec:
  replicas: 4
  selector:
    matchLabels:
      env: dev
  template:
    metadata:
      name: first-depo
      labels:
        env: dev
    spec:
      containers:
      - image: nginx
        name: nginx-container
